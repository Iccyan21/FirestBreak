# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'FirstBreak' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # 以下の行を削除
  # pod 'SpotifyiOS'  

  target 'FirstBreakTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FirstBreakUITests' do
    # Pods for testing
  end
end